配置环境：
python=3.9
conda install pytorch==2.1.0 torchvision==0.16.0 torchaudio==2.1.0 pytorch-cuda=11.8 -c pytorch -c nvidia 【linux环境指令，其他方法：https://pytorch.org/get-started/previous-versions/，版本别错就行】
conda install -c dglteam/label/th21_cu118 dgl 【linux环境指令，其他方法：https://www.dgl.ai/pages/start.html，版本别错就行】
pip install transformers
【提示缺什么装什么：pip install ...】
